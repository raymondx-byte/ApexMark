<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Overlay 权限（悬浮球） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 前台 Service 权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 存储（旧版导出兼容） -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".ApexMarkApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ApexMark"
        tools:targetApi="34">

        <!-- ═══ 主入口 ═══ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.ApexMark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- 支持接收分享文本 -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/markdown" />
                <data android:mimeType="text/x-markdown" />
            </intent-filter>
        </activity>

        <!-- ═══ 透明转换 Activity（仅当 app 不在前台时使用，避免任务切换动画） ═══ -->
        <activity
            android:name=".service.ClipboardConvertActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleInstance"
            android:taskAffinity=":apexmark.bridge"
            android:theme="@style/Theme.Transparent" />

        <activity
            android:name=".service.NotificationMenuActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=":apexmark.bridge"
            android:theme="@style/Theme.NotificationMenu" />

        <!-- 悬浮球：取焦后判型再弹菜单（须注册，否则 startActivity 静默失败） -->
        <activity
            android:name=".service.ClipboardPeekActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleTop"
            android:taskAffinity=":apexmark.bridge"
            android:theme="@style/Theme.NotificationMenu" />

        <!-- ═══ 悬浮球前台 Service ═══ -->
        <service
            android:name=".service.FloatingPortalService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- ═══ 快捷指令广播接收器 ═══ -->
        <receiver
            android:name=".receiver.QuickActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.apexmark.ACTION_CONVERT_CLIPBOARD" />
                <action android:name="com.apexmark.ACTION_TOGGLE_BUBBLE" />
            </intent-filter>
        </receiver>

        <!-- ═══ FileProvider ═══ -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
